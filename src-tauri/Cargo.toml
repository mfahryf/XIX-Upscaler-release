[package]
name = "xix-upscaler"
version = "0.1.0"
description = "XIX-Upscaler - Winamp-themed batch image upscaler"
edition = "2021"

[lib]
name = "xix_upscaler_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-opener = "2"
tauri-plugin-process = "2.2"
tauri-plugin-updater = "2.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls-no-provider", "rustls-tls-webpki-roots-no-provider", "system-proxy", "stream", "json", "multipart", "socks"] }
uuid = { version = "1", features = ["v4", "serde"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12", "logging"] }
rustls-pki-types = "1"
webpki-roots = "1"
sha2 = "0.10"
x509-parser = "0.18"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync", "net", "io-util"] }
futures-util = "0.3"
parking_lot = "0.12"
regex = "1"
base64 = "0.22"
ed25519-dalek = { version = "2", features = ["rand_core"] }
rand_core = { version = "0.6", features = ["getrandom"] }
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "webp"] }
ort = { version = "2.0.0-rc.13", default-features = true, features = ["directml"] }
windows = { version = "0.58", features = ["Win32_Security_Cryptography", "Win32_Foundation", "Win32_UI_Shell", "Win32_UI_WindowsAndMessaging", "Win32_Storage_FileSystem"] }

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
debug = 0
